/**
 * @citesync/docx — placeholder barrel.
 *
 * The public parseDocument(buffer) -> AcademicDocument API and typed security
 * errors (NotADocxError, ZipBombError, ...) land in S01-T3..T6.
 * Kept as a minimal module stub so the workspace compiles end-to-end.
 */
export {};
